import Calendar from "../components/Calendar/calendar";

const Reservation = () => {
  return (
    <div>
      <header>
        <h1>Reservations:</h1>
      </header>

      <Calendar />
    </div>
  );
};

export default Reservation;
